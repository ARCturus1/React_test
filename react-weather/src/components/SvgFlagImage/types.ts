/**
 * Type definition for the SvgFlagImage component.
 * 
 * @property countryCode - The ISO 3-letter country code (e.g., "US", "FR")
 * @property className - Optional CSS class name to apply to the container
 */
export type SvgFlagImageProps = {
  countryCode: string;
  className?: string;
};
